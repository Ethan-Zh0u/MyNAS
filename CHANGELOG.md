# 更新日志

本项目遵循语义化版本规则，所有可识别的发布版本都记录在这里。

## [0.9.4] - 2026-08-11

### 修复

- 修复上传重试已把服务端 mapping 收敛到目标 asset 后，进程内仍用上传前缓存的设备 mapping 页面覆盖新结果，导致“全部”页面在本次运行期可能继续显示一个旧关联副本的问题。前台上传和系统后台 outcome 完成后都会取消旧 mapping 请求、清空旧快照并重新读取服务端权威映射；刷新失败时保留刚完成的本地结果，并继续对自动工作失败关闭。

### 验证

- 新增回归模拟上传前旧 mapping 与上传后 canonical mapping 的切换，确认服务端 mutation 会废弃旧快照并把持久队列恢复到 canonical asset。

## [0.9.3] - 2026-08-11

### 修复

- 修复 MyNAS 远端详情预览在服务端衍生 JPEG 的方向或尺寸与原始媒体元数据不一致时，按原始宽高预留出大片空白的问题。界面现在先采用服务端返回的 preview/grid 尺寸，并在解码完成后以实际位图尺寸重新布局。
- 修复 Tailscale 已恢复、但删除前 health 探测仍处在建连窗口时被 4/6 秒短超时误判为“未连接”的问题。探测现在复用图库/连接请求的直连私网会话与 12/30 秒预算；不可达、检查中或最终动作时的失败关闭与服务端 owner/version/resource-group 校验保持不变。
- 修复部署脚本只更新根目录 `VERSION`、但后端健康检查仍编译入旧字符串的问题。发布二进制现在在构建时从唯一的 `VERSION` SemVer 源注入 health/capabilities；不规范版本会在部署前失败。
- 修复 MyNAS 原件下载进 Photos 后因上传期 `clientResourceID` 或 `image/png`/`public.png` 表达不同而被错误创建为第二个远端 asset、随后显示“完整性校验失败”的问题。关联 manifest 现在携带已核验目标 asset ID；服务端在创建会话前复核 owner、卷、source committed 状态及全部资源角色、字节数和 SHA-256，匹配时直接建立目标 mapping，不匹配时以 422 失败关闭。
- 普通上传的内容去重也新增完整资源证明回退，不再把传输 ID、原文件名或 MIME/UTI 拼写当作内容身份。历史上因旧传输指纹误建的完全相同 asset 会把映射迁移到用户已验证的目标，并标记为可回溯的 `sourceSuperseded`；原件文件和资源元数据保持不删。
- 修复历史自动备份 `.waiting` 记录已不在当前 PhotoKit 访问范围时仍被当作可运行任务，造成协调器反复启动空队列、页面长期显示正在处理且失败重试被拒绝的问题。自动评估现在只计入当前可访问且源版本匹配的项目；用户重试会立即持久化为等待，并在已有上传或 mapping 恢复释放协调器后继续，不因瞬时忙碌而丢失。

### 验证

- Xcode 27 beta 的 iPhone 17 Pro 模拟器完整 `MyPhotosTests` 73/73 通过；新增 iOS 回归确认 verified association 会把目标 asset ID 写入上传 manifest、过期自动等待记录不会制造空运行，以及协调器忙碌时手动重试仍会持久化并保留目标 asset ID。后端完整测试通过，新增回归覆盖跨 transport ID/MIME 表达去重、目标一致关联、目标不一致拒绝，以及历史完全相同 asset 的可回溯合并且资源仍保留。
- 早期候选 `56a3b57` 因 iPhone 16 Pro 发现“预期 asset A、服务端返回 asset B”的完整性失败而作废；`d56790e` 随后暴露历史自动等待记录会持续占用协调器。最终提交 `1c82ec4` 已从干净 worktree 原子部署到树莓派 release `20260811T030439Z`，health/capabilities 精确回读 `0.9.3`，同提交签名 App 已覆盖安装。用户单次点击“仅重试 2 个失败项目”后，持久队列由 17/19 和 2 个失败达到 19/19、0 失败；两条本机记录均指向 `ast-ecb89fc9b3efe63e5195f3948db9105d`，服务端回读为 `sourceCommitted`、衍生预览 ready、2 台设备共 3 条精确 mapping。真机验收通过并发布 `v0.9.3`。

## [0.9.2] - 2026-08-06

### 新增

- I3 新增独立、默认关闭的本地 OCR 文字索引。用户必须先保留 I2 的端侧像素分析许可，再在“设置 > 端侧分析与本地索引”明确允许、更新、清空或关闭删除当前账号的文字索引。
- 仅对当前 Photos 权限范围内、本机可取得的静态图片调用 Apple Vision；一次只处理一张、渲染上限为 2,048 px。视频和 Live Photo 被排除；iCloud-only 图片保持未索引，不触发原件下载。
- 修复 OCR 搜索结果打开照片详情再返回时被界面重载清空的问题；同一账号现在保留查询并重新读取本地索引，切换账号或撤回许可仍会清空状态。
- “照片”主页只有一个本地搜索框，同时检索 I1 元数据和已允许的 I3 OCR，按 asset ID 去重后以每行三张、可直接点入详情的纯照片缩略图网格显示；不显示 OCR 原文、索引来源或重复标题。“人物”栏只保留未来的人物识别、人物集合和对应照片。

### 安全

- OCR 原文仅以 Data Protection 原子 JSON 保存于当前账号的 `AppCache/<server>/<user>/analysis-queue`，绑定 schema、OCR 许可 revision、Vision processor revision 和 `accountID + serverID + userID`。错账号复制、损坏、重复标识和不支持版本均失败关闭。
- 单独关闭 OCR 只删除文字索引；撤回 I2 父许可、修复不可读父许可或清理当前账号缓存会删除全部 OCR 派生数据。OCR 不上传 MyNAS 或任何中心化服务，也不包含人物、物体、embedding 或语义结果。

### 验证

- Xcode 27 beta 的 iPhone 17 Pro 模拟器完整 `MyPhotosTests` 59 项通过，其中新增回归覆盖 I1/I3 交集去重、顺序与媒体类型；既有 7 项 I3 回归继续覆盖双重 opt-in、最小持久化字段、增量/不可本机读取项失效、账号隔离、父许可撤回、损坏/重复索引失败关闭与详情返回后的搜索状态保持。
- 取代此前“人物页 OCR 搜索”候选的提交 `f8352d2` 已部署为树莓派 release `20260806T075201Z`；独立反读为 systemd active、health/capabilities `0.9.2`、`photoDelete=true`、`backgroundTransfers=true`，其签名 Debug App 已在 iPhone 16 Pro 验收。该同一提交的 annotated `v0.9.2` tag 与 GitHub Release 已发布，I3 完成。

## [0.9.1] - 2026-08-06

### 新增

- I2 新增独立、默认关闭的端侧像素分析许可和当前账号的本地待分析队列。明确许可前拒绝建队；许可后，I2 仅用当前 Photos 权限范围内的元数据生成 PhotoKit 标识符与源版本清单。
- 人物页现在显示许可、待分析数量、最近准备时间、手动更新和“关闭并删除”控制。关闭许可或清除当前账号缓存会删除队列；不会删除系统照片、MyNAS 原件或 I1 本地搜索索引。

### 安全

- I2 队列以 Data Protection 原子写入 `AppCache/<server>/<user>/analysis-queue`，文件内绑定 schema、许可 revision 与 `accountID + serverID + userID`。错账号复制、损坏、重复标识和不支持版本均失败关闭。
- 本阶段不链接 Vision/Core ML 处理器，不读取、缓存或上传照片/视频像素，不下载 iCloud 原件，也没有 OCR、物体、人物、embedding 或语义结果。

### 验证

- Xcode 27 beta 的 iPhone 17 Pro 模拟器完整 `MyPhotosTests` 51 项通过，其中 7 项 I2 回归覆盖默认拒绝、最小持久化字段、增量队列、账号隔离、错账号复制拒绝、关闭/账号缓存清理和损坏/重复队列失败关闭。
- 候选提交 `bf75153` 已原子部署为树莓派 release `20260806T042351Z`，systemd active、health/capabilities 精确回读 `0.9.1`，并保留 `photoDelete=true` 与 `backgroundTransfers=true`；同一提交的签名 Debug App 已在 iPhone 16 Pro 验收，annotated tag `v0.9.1` 与 GitHub Release 已发布。

## [0.9.0] - 2026-08-06

### 新增与修复

- MyNAS Photos 的 H1/H2 已完成真机验收：原件下载/导入和 MyNAS-only 删除继续以完整资源组、同源、字节数与 SHA-256 校验为前提；系统分享只交付已验证原件，完成、取消或中断后清理私有临时下载组；当前账号的可复用缓存按 LRU 管理并可由用户明确清除。
- 远端永久删除在展示、回到前台和最终动作边界均重新验证当前账号的 Tailscale 私网 health。不可达时所有 MyNAS 删除操作保持禁用；纯本机 PhotoKit“最近删除”不受影响。
- I1 新增默认关闭、账号隔离、可清除的端侧元数据索引，可在当前 Photos 权限范围内检索媒体类型、日期、收藏、RAW 与尺寸。索引不读取像素、不请求 iCloud 原件，也不调用 MyNAS；命中结果仅按需显示禁止网络访问的本地缩略图和详情入口。
- 修复收藏等 PhotoKit 元数据变化被误判为内容变化的问题：只有系统明确报告 `assetContentChanged == false` 时才推进本地观察版本，已确认备份不会重新排队或在统一时间线重复；无法分类的变化仍失败关闭并重新备份。

### 验证

- Xcode 27 beta 的 iPhone 17 Pro 模拟器完整 `MyPhotosTests` 44 项通过；I1 包含默认关闭、隔离、损坏恢复、清空/关闭以及收藏元数据不重传/不重复回归。
- iPhone 16 Pro 已通过 I1 的结果展示、收藏切换、清空重建、关闭重启用与轻量资源观察；H1/H2 的下载、分享、删除和账号隔离缓存流程也已通过。
- 2026-08-06 已由提交 `85cd4f2` 原子部署到树莓派 release `20260806T032331Z`；systemd active，health 与 capabilities 均回读 `0.9.0`，保留 `photoDelete=true` 与 `backgroundTransfers=true`。同一提交已在 iPhone 16 Pro 安装、启动并通过最终验收，随后发布 annotated `v0.9.0` GitHub Release。

## [0.8.6] - 2026-08-05

### 新增与改进

- MyNAS Photos 的自动备份策略与系统传输基础继续完善：每账号策略、Wi-Fi/蜂窝与低电量门槛、受保护传输登记册、file-backed background `URLSession` 及受限 BGProcessing 恢复入口均保持可解释状态和失败关闭语义。
- 远端图库新增完整原件组下载前的同源、字节数和 SHA-256 校验；本机存在候选时必须先核验并关联，或由用户明确确认创建另一份副本。
- 新增 MyNAS-only 删除与受精确设备映射限制的“同时删除本机照片”流程。前者永不触碰本机 Photos；后者固定先由 PhotoKit 移入“最近删除”，再请求 MyNAS 删除。
- 统一时间线的“统一时间线”开关及 Limited Photos 权限说明移至设置；主页不再长期占用显示范围切换或汇总卡。

### 安全

- 删除请求继续由服务端复核 owner、当前 asset version、完整资源组、映射基数和衍生 worker 状态；共享、旧版本或处理中资源会被拒绝。
- 后台传输任务持续绑定账号、服务器、用户、卷与当前源版本；传输进度不等同 MyNAS 已确认完成。

### 部署与验证

- 23 项前端测试、TypeScript/生产构建、后端 `go test ./...`、`go vet ./...` 和 Linux ARM64 构建通过。
- 2026-08-05 已将该后端/Web 版本原子部署到树莓派；health 正常，capabilities 回读 `photoDelete=true` 与 `backgroundTransfers=true`。
- 当前 iOS H1 下载／删除真实媒体流程仍待设备验收；完整 iOS test build 受本机 CoreSimulator/Icon Studio 图标导出服务阻塞，不能把该环境故障误记为通过。

## [0.8.5] - 2026-07-27

### 修复

- 修复 MyNAS 图库对 HEIC/HEIF 的错误预览：FFmpeg 有时会选择容器内的 tile 或预览流，造成黑白、黑屏或与原照片不符的缩略图和详情图。衍生 worker 现在先通过 Libheif 的 `heif-convert` 提取主图，再生成 `tiny`、`grid` 和 `preview`。
- 派生策略升级为 `photos-browse-v2`。已提交但仍使用旧策略的资产会被安全地重新排队生成预览；原始照片、视频、Live Photo 配对资源和既有 asset 映射不会被修改。
- “全部”统一时间线在可见期间每 30 秒检查 owner-scoped 变更；收到远端更新后刷新远端项目，避免必须退出再进入才能看到新预览。

### 改进

- 树莓派安装与 macOS 部署预检新增 `heif-convert` 依赖检查，缺少 HEIC/HEIF 主图解码器时会明确失败，而不会生成错误预览。

### 验证

- 后端 `go test ./...`、`go test -race ./...`、`go vet ./...` 和 Linux ARM64 构建通过。
- 2026-07-27 创建部署前 SQLite 快照后，MyNAS 0.8.5 已原子部署；55/55 旧版衍生图按 v2 重建为 ready。
- 通过真实 HEIC/Live Photo 资源验证：重新下载的 preview 显示主照片内容，而非容器内部 tile 或错误黑白预览。

## [0.8.4] - 2026-07-27

### 改进

- MyNAS Photos 备份队列新增持久化失败分类，区分网络、本地原件、iPhone/MyNAS 空间、完整性、身份、服务和配置问题，并给出对应处理建议。
- 备份页新增独立失败区和居中的“仅重试失败项目”入口；定向重试只重置当前可访问的失败任务，已经完成的任务保持不变。
- 网络错误以及 HTTP 408、429、5xx 采用退避续传；完整性和配置等终态错误停止自动循环，等待用户处理后手动重试。
- 大队列默认只渲染最近 50 个非失败任务，失败区默认显示最近 100 项，避免相册很大时一次构造全部队列行。
- 照片页新增独立的只读“MyNAS 图库”入口；远端项目不与本地 PhotoKit 时间线提前混合。
- 远端图库支持稳定分页、元数据 ETag/304、网络失败时离线回退、正方形缩略图、双指 2–10 列、详情 preview，以及 Live Photo、RAW/ProRAW 和视频标记。
- grid/preview 下载校验服务端 SHA-256，并写入 `serverID/userID` 账号隔离缓存；服务端相对下载地址必须保持同源。
- MyNAS 远端网格不显示重复的绿色备份对勾；仅对尚未 browse-ready 的项目显示处理中提示。
- MyNAS 视频与 Live Photo 配对视频使用 AVFoundation 的按需 Range 读取；资源加载器复用 MyNAS 的无代理网络通道，避免 Simulator/Mac 的 PAC 代理截获私有 Tailscale 流。
- 服务端把 PhotoKit 的 `com.apple.quicktime-movie`、`public.mpeg-4` 等 UTI 转为标准 HTTP MIME（如 `video/quicktime`、`video/mp4`），供 AVFoundation 识别；原始资源元数据仍保留原 UTI。
- 远端详情新增只读原件资源清单，并在图库可见期间以 owner-scoped `/changes` cursor 建立基线和提示更新；不自动导出、更改或删除 MyNAS 原件。
- 主照片页新增原生“全部 / 本机”切换和只读统一时间线：仅在当前设备的 server-confirmed backup job asset ID 与当前本地源版本一致时合并；其余项目明确标为仅本机、备份状态或仅 MyNAS，不以时间和缩略图猜测去重。
- F4 工作区实现新增显式版本转移：同一设备的同一 PhotoKit 本地标识切换到另一完整资源组后，MyNAS 保留两份原件并返回匿名的前序/后续关系数量；Live Photo 必须整组提交。
- H0 工作区实现新增本机照片多选删除与可选 MyNAS 备份永久删除：iOS 使用系统 PhotoKit 批量请求，把本机项目交给“最近删除”；仅在本机删除成功后，MyNAS 才会永久删除同一完整资源组。若用户从系统“最近删除”找回本机项目，可重新备份。

### 安全

- F4 只记录已验证的同设备映射切换，不按文件名、时间、缩略图或跨设备相同内容推断版本；设备 ID、本地标识和前后 fingerprint 永不出现在浏览 API 响应中。
- H0 的 MyNAS 永久删除默认关闭，并由服务端再次验证当前本机版本、唯一 owner 映射和完整资源组；网络或服务端失败不会撤销已完成的系统“最近删除”，也不会误报 MyNAS 删除成功。

### 验证

- Xcode 27 beta 对 iPhone 17 Pro 模拟器完整构建通过；旧版无失败字段的 45 项持久化队列成功迁移。
- 注入一项可恢复网络失败后，界面正确显示 44/45、98% 和专用重试入口；重试后恢复 45/45，原模拟器队列随后按备份副本还原。
- iPhone 17 Pro 模拟器通过真实 MyNAS 读取 45/45 browse-ready 项、45 张 grid 和 1 张详情 preview；缓存元数据含服务器 ETag，并识别 1 个 Live Photo 与 1 个 DNG/ProRAW。
- 2026-07-25 已以部署前 SQLite/原件/衍生 SHA-256 快照完成 0.8.1 热修复发布；同一 12.8 MB MOV 的 Range 响应从内部 UTI 改为 `video/quicktime`，并在 iPhone 17 Pro 模拟器从 MyNAS 成功渲染视频首帧。
- 同一模拟器还以真实 Live Photo 验收静态 HEIC 与 paired MOV：点击“播放实况片段”后成功渲染视频帧；图库 change cursor `91` 的条件请求返回 `304`，首次基线不会误报历史更新。
- 2026-07-26 通过 iPhone 17 Pro 模拟器连续备份两项独立测试 PNG，验证本地和 MyNAS 图库从 45 项达到 47 项；第二次前台增量显示“有更新”提示，后续重新加载网格才读取第 47 项，不会打乱分页位置。
- 发布前创建 `pre-0.8.4-h0-f4-20260727T035006Z` SQLite 一致性快照，`PRAGMA integrity_check` 为 `ok`；SHA-256 清单记录 48 个原始资源和 141 个 ready 衍生文件。前端 23/23 测试、生产构建、后端 `go test ./...`、`go vet ./...` 与 `go test -race ./...` 通过，ARM64 已原子切换至 `0.8.4`。
- iPhone 17 Pro 模拟器完成真实 H0 删除验收：唯一 1200 × 1200 测试资源组先安全入库，再经系统确认移入 iOS“最近删除”；MyNAS `/assets` 数从 49 回到 48，专用测试资源不再存在。重复内容的两条设备映射会被服务端拒绝永久删除，未发生误删。
- 同一 iPhone 17 Pro 模拟器完成 H0 恢复回归：从系统“最近删除”还原上述测试图后，客户端由 49 项检测到 50 项并以正常管线完成 50/50 备份；MyNAS 产生新的单映射资源组（`sourceCommitted`、三项衍生文件均 ready、可浏览），未把已永久删除的旧映射误报为成功。
- iPhone 17 Pro 模拟器完成真实 F4 普通照片回归：唯一原版先入库，系统相册保存一次曝光调整后 MyNAS 从 49 个资源组增至 50 个；编辑版保留 `photo + fullSizePhoto + adjustmentData`，原版未被覆盖。恢复原始状态后两端均返回一条前序和后续版本关系，统一时间线正确显示本机当前版与仅 MyNAS 的编辑版。

## [0.8.3] - 2026-07-27

### 新增

- F3 新增精确内容关联摘要：仅当同一账号、同一卷的完整资源组 manifest 严格一致时，详情与统一时间线才展示匿名的关联设备/备份记录数量；不会合并、隐藏、删除或修改任一设备的照片记录。

### 安全

- 关联 API 不返回 fingerprint、其他设备 ID、其他设备的本地标识或存储路径；相同文件名、相近时间和缩略图相似均不能建立关联。

### 验证

- 发布前创建 `pre-f3-relationships-20260727T021134Z` SQLite 一致性快照，`PRAGMA integrity_check` 为 `ok`；SHA-256 清单记录 48 个原始资源和 141 个 ready 衍生文件。
- 前端 23/23 测试、TypeScript/生产构建、后端 `go test ./...` 与 `go vet ./...` 通过；ARM64 版本已原子切换至 `0.8.3`。
- 已部署服务的 owner-scoped 列表与详情均返回关联摘要字段，且验收响应不含 fingerprint 或 device ID。

## [0.8.2] - 2026-07-27

### 新增

- MyNAS Photos 新增受 owner 与 device ID 双重隔离的已验证 asset 映射读取接口。iOS 在普通卸载重装后可用持久设备标识恢复本机 PhotoKit 项目的备份关联，不必重新上传原件。

### 安全

- 映射接口仅返回恢复所需的 local identifier、asset ID、源版本和已确认状态；不返回内容 fingerprint、媒体路径或其他设备、账号的记录。iOS 仅在本地 PhotoKit 源版本严格匹配服务器记录时恢复完成状态，其余项目继续走正常备份队列。

### 验证

- 发布前创建 `pre-f2-mapping-20260727T012037Z` SQLite 一致性快照，`PRAGMA integrity_check` 为 `ok`；SHA-256 清单记录 48 个原始资源和 141 个衍生文件。
- 标准发布门禁通过：前端 23/23 测试、TypeScript/生产构建、后端 `go test ./...` 与 `go vet ./...`；ARM64 版本已原子切换至 `0.8.2`。
- 已部署服务的 capabilities 返回 `deviceAssetMappingRecovery=true`；以独立部署检查身份查询不存在的设备 ID 返回空分页结果，服务保持 active。

## [0.8.1] - 2026-07-24

### 修复

- DNG/ProRAW 衍生任务优先使用 LibRaw `simple_dcraw -E` 提取文件内嵌的全尺寸 JPEG，再由 FFmpeg 生成 `tiny`、`grid` 和 `preview`；原始 DNG 不转码、不替换，并在提交衍生结果前重新核对 SHA-256。
- 树莓派安装和部署预检现在同时要求 `ffmpeg` 与 `simple_dcraw`，避免特殊格式在上线后才发现依赖缺失。

### 验证

- 真实 IMG_4074.DNG（91,483,916 字节）已由失败状态恢复为 ready，三档衍生图可授权读取，原件 SHA-256 保持不变。
- 当前数据库 45/45 asset ready、46 个原始资源、135 个衍生文件，SQLite 完整性正常；pre-E3 原件清单 46/46 通过。

## [0.8.0] - 2026-07-24

### 新增

- MyNAS Photos 新增 owner-scoped 远程图库 API：稳定分页的 asset 列表与详情、连续 `changes` 游标、tiny/grid/preview 和多资源 original 下载。
- metadata 和文件读取支持 ETag/304；原件与视频支持 Range/206；响应不暴露卷挂载路径或服务器存储路径。
- Live Photo 可分别授权读取静态照片与 paired video；`photo_changes` 在上传提交及衍生状态变化时写入 upsert。

### 安全与验证

- 跨账号 asset 详情和文件读取保持不可见，空图库和 404 行为通过自动化及真实树莓派验证。
- 当前 45 项真实资产完成 5 页分页、增量变化、条件请求、Range、路径不泄露和 Live Photo 双资源验收。

## [0.7.0] - 2026-07-24

### 新增

- 健康接口新增树莓派实时 CPU 温度，读取 Linux thermal zone，无法读取时省略数值而不伪造 `0°C`。
- MyNAS 网页在当前设备、主页实时状态和设置页显示温度，每 5 秒刷新。
- MyNAS Photos 的设置页在当前服务器旁显示实时温度；iOS 26/27 使用原生 Liquid Glass 温度徽章，旧系统回退到 Material。
- MyNAS Photos 首页备份进度卡片新增已上传大小和备份总大小；仍在读取 PhotoKit 资源大小时明确显示待统计项目数，不用估算值冒充总量。

## [0.6.0] - 2026-07-24

### 新增

- MyNAS Photos 新增持久化照片派生任务：原件提交后由树莓派后台生成 `tiny`、`grid` 和 `preview` 三档 JPEG 预览。
- 派生任务支持失败退避、服务重启恢复、输出完整性校验和幂等重跑；只有全部预览通过校验后，asset 才进入可浏览状态。
- 生成预览前后都会校验所选原始资源的 SHA-256；Live Photo、HDR、RAW/ProRAW 等原始资源保持原字节不变。

### 已知边界

- 本版尚未提供远程照片读取 API 和统一时间线，iOS 端仍不能浏览服务器生成的预览。
- 当前测试媒体没有 DNG/ProRAW 样本，DNG 解码兼容性仍需用真实样本补充验证。

## [0.5.0] - 2026-07-24

### 新增

- MyNAS Photos 首版私有连接：Tailscale `*.ts.net` 地址校验、二维码配对、稳定用户/服务器 ID 和可选存储卷。
- 手动原始资源备份：按完整 PhotoKit asset 导出全部资源，支持 Live Photo、HDR、RAW/ProRAW、4 MiB 分片、SHA-256、续传、按用户和卷去重与服务端原件提交。

### 已知边界

- 该版本的“上传完成”仅表示原始资源已安全入库；缩略图/预览、远程照片浏览、自动备份、恢复/删除和灾难恢复尚未完成，不能称为完整可浏览备份。
- MyNAS Photos 的详细状态和验收标准见 `docs/myphotos/IMPLEMENTATION_PLAN.md`。

## [0.4.0] - 2026-07-23

### 改进

- 网页端整体换用浅色纯净科技风设计：浅灰背景、白色面板、蓝色主色、圆角卡片与柔和投影；深色主题保留并改为同一结构的深色变体。
- 主页新增总容量、已用、在线硬盘与实时吞吐四格统计行。
- 硬盘卡片重新设计：在线/离线状态徽章、大号用量百分比、渐变容量条（用量达到 75% 转为橙色警示）、实时读写速率与离线灰态。
- 界面字体换为 Inter 与 JetBrains Mono，随前端本地打包，离线环境正常显示。
- 容量格式化新增 TB 档位，多块大容量硬盘不再显示为超长的 GB 数字。

### 验证

- 前端 18 项测试、TypeScript 检查与生产构建均通过。
- 浅色、深色、移动端布局与连接页均已截图核对。

## [0.3.2] - 2026-07-21

### 改进

- 后端启动时校验 `/mnt/nas` 真实挂载点，硬盘掉线时进入安全模式而不误写系统盘。
- 新增数据盘挂载自动重试、UUID 校验和健康接口存储状态，前端会区分“硬盘未挂载”和“Tailscale 未连接”。
- NTFS 异常只自动检测和重试挂载，不会后台静默执行高风险文件系统修复。

## [0.3.1] - 2026-07-16

### 改进

- 将 `https://mynas-rsp.pages.dev/` 明确为唯一推荐的用户入口，Tailscale `*.ts.net` 地址仅作为后台数据 API。
- 私有 API 连接失败时新增代理冲突诊断，可根据当前 tailnet 生成 Clash/mihomo Fake-IP 与 `DIRECT` 绕过规则。
- 连接页面支持复制兼容规则、重新检测，并提供中英文说明。
- README 和新手指南明确建议关闭 Clash Verge、Surge 等 VPN/代理的 TUN、增强模式或 Fake-IP；必须共存时再配置绕过规则。
- 本地开发显式设置 `VITE_API_URL` 时可连接远程树莓派 API，不再被 localhost 默认值覆盖。

### 修复

- 修复用户直接打开私有地址时无法加载诊断界面的错误引导；界面不再提供“打开私有地址”入口。
- 非 Tailscale 地址不会显示不适用的代理绕过规则。

### 验证

- 前端 18 项测试、TypeScript 检查、生产构建、Go 测试、vet 和构建均通过。
- Cloudflare Pages 正式入口已发布并验证包含新版代理诊断文案。

## [0.3.0] - 2026-07-14

### 新增

- 上传继续使用 8 MB 分块，传输页面新增当前分块的浏览器级实时进度，已上传大小不再只按 `8 MB → 16 MB` 跳动。
- 新增 MyNAS 应用级读写字节计数；主页按真实采样间隔显示 `B/s`、`KB/s` 或 `MB/s`。
- 上传暂停、取消或断线时，界面回退到服务器已确认的分块边界，避免把尚未落到服务端的数据误认为已完成。

### 修复

- 修复硬盘详情保存旧采样对象，导致“旧值减新值”后读写速率长期显示为 0 的问题。
- 修复将两秒累计量显示成 `/2s` 而不是实际每秒速率的问题。
- 修复 Linux 页缓存延迟刷新物理块设备计数，导致上传期间绝大多数采样为 0 的问题；现在统计 MyNAS 实际接收和发送的数据。

### 验证

- 使用约 40 MB 文件完成真实 Tailscale 分块上传，文件成功校验并出现在 NAS 文件列表中。
- 连续 8 个 1 MB 探针分块中，服务端 `writeBytes` 每次准确增加 `1,048,576` 字节。
- 前端 15 项测试、TypeScript 检查、Go 测试、Windows/Linux ARM64 构建以及 Cloudflare Pages 发布均通过。

### 后续计划

- 增加账户退出与切换入口：清理当前 MyNAS 会话和设备状态，并支持切换 Tailscale 账号或未来的本地账号，避免用户只能手工清理浏览器状态。

## [0.2.0] - 2026-07-14

### 新增

- 新增多硬盘架构，分别展示每块硬盘的容量、已用空间、可用空间、在线状态和设备详情。
- 新增 `sudo mynas-setup` 树莓派接盘向导，支持 `ext4`、`NTFS3`、`exFAT`，自动排除系统盘并对格式化操作进行二次确认。
- 新增硬盘重命名，名称写入树莓派数据库并在重启后保留。
- 新增跨硬盘复制、移动、上传和按卷隔离的回收站。
- 新增多 MyNAS 设备管理，可添加、命名、重命名、切换和移除多台树莓派。
- 新增首次连接引导，覆盖 SSH 服务、电脑端终端位置、Tailscale 安装登录和多设备识别。
- 新增 Windows、macOS、Linux 操作系统切换和中文/英文界面。
- 新增主页深色主题开关、目录返回上级按钮和多硬盘演示模式。
- 新增面向零基础用户的接线与安装总指南，包含路由器网线、硬盘盒供电和硬盘连接安全说明。

### 改进

- 统一文件悬浮操作图标大小并重新设计命令复制按钮。
- 已配对设备后续访问不再重复显示首次安装教程；离线时显示恢复连接步骤。
- 上传任务支持暂停、继续、取消和离开页面后的状态恢复。
- 配置文件刷新不再覆盖用户自定义的硬盘名称。

### 当前限制

- 通用的一键树莓派初装程序尚未完成；当前生产部署脚本仍面向项目维护者的测试设备。
- 远程访问和身份认证仍以 Tailscale 为默认安全边界。

## [0.1.0] - 2026-07-13

### 已有功能

- 提供 React + Go 的个人 NAS 管理界面。
- 支持文件浏览、上传、下载和基础文件操作。
- 提供磁盘容量、读写状态和审计记录。
- 使用 Tailscale Serve 作为当前远程访问和身份认证边界。
- 提供 Windows 本地开发、Linux ARM64 构建和树莓派部署脚本。

### 已知问题

- 首页文件夹的快速操作按钮尺寸不一致。
- 部署配置仍与当前树莓派、挂载路径和 Tailscale 环境绑定。

### 后续计划

- 通用树莓派与外接硬盘快速部署。
- 视频在线预览。
- 可选的本地账号密码登录。
